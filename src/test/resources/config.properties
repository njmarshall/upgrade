# ============================================================
# Test Configuration — Upgrade Loan App Test Framework
# ============================================================
# This file contains safe defaults only — no real credentials.
# For real values, copy to config.local.properties and fill in.
# config.local.properties is git-ignored and never committed.

# --- API Endpoint ---
api.loanapp.url=https://credapi.credify.tech/api/brfunnelorch/v2/resume/byLeadSecret

# --- UI Endpoints ---
ui.loanapp.url=https://www.credify.tech/funnel/nonDMFunnel
ui.portal.login.url=https://www.credify.tech/portal/login

# --- Test Data IDs (replace in config.local.properties) ---
testdata.existing.loan.app.id=REPLACE_WITH_REAL_UUID
testdata.nonexisting.loan.app.id=00000000-0000-0000-0000-000000000000

# --- Browser Config ---
# Set to true for CI / headless environments
browser.headless=false
